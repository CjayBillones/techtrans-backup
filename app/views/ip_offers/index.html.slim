/ #ipoffersbg
/   .container
/     col-lg-12
/       .well
/         h1
/           | IP Offers Listing
/     .border-top
/       - @ip_offers.each do |ip_offer|
/         .row.top-buffer.border-bottom
/           .col-md-3.vcenter
/             a href='#'
/               img-responsive
/                 - if ip_offer.photo.present?
/                   = image_tag ip_offer.photo.url, :size => "250x200"
/                 - else
/                   = image_tag 'placeholders/no_image_general.jpg', :size => "250x200"
/           .col-md-9.vcenter
/             h3
/               = ip_offer.title
/             h4
/               | Posted by 
/               = fullname(User.find(ip_offer.user_id)) + ' | '
/               = local_time(ip_offer.created_at)
/             p
/               = simple_format(truncate(ip_offer.overview, length: 500))
/               = link_to ip_offer, class: "btn btn-primary btn-lg" do 
/                 | View IP Offer
/                 span.glyphicon.glyphicon-chevron-right
/               .bottom-buffer

.ui.container style="margin-top: 70px;"
  h2.ui.header.center.aligned IP Offers Listing
  .ui.search style="margin-bottom: 20px;"
      .ui.icon.input style="width: 100%;"
        input.prompt type="text" placeholder="Search"
        i.search.icon
      .results
  .ui.grid
    - @ip_offers.each do |ip_offer|
      .row.ui.basic.segment
        .five.wide.column
          .ui.image.left.floated.middle.aligned.medium
            - if ip_offer.photo.present?
              = image_tag ip_offer.photo.url, :size => "250x200"
            - else
              = image_tag 'placeholders/no_image_general.jpg', :size => "250x200"
        .eleven.wide.column
          h3.ui.header 
            .content
              = ip_offer.title
            .sub.header
              | Posted by 
              = fullname(User.find(ip_offer.user_id)) + ' | '
              = local_time(ip_offer.created_at)
          p 
            = simple_format(truncate(ip_offer.overview, length: 500))
            = link_to ip_offer, class: "ui button basic blue" do
              | View IP Offer &nbsp;
              i.fa.fa-chevron-right